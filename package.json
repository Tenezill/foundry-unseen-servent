{
  "name": "foundrys-unseen-servant",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@11.10.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "test:e2e": "pnpm --filter @companion/gateway test:e2e",
    "dev:gateway": "pnpm --filter @companion/gateway dev",
    "dev:web": "pnpm --filter @companion/web dev"
  }
}
